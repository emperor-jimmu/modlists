#import "../helpers.typ": *

==== Wave 4: Modlist

Wave 4 assumes the full foundational toolset — Navigraph (current nav data and charts are now load-bearing), LittleNavMap (briefing and planning), AviTab (plates in the cockpit), Better Pushback (gate departures) — plus everything added in Waves 1-3.

The wave's centerpiece is a *user-supplied mod*: the Boeing 737-800 in the form of the Zibo Mod B737-800X you supplied.

#mod-entry(
  "Zibo Mod B737-800X",
  url: "https://x-plane.to/file/309/zibo-mod-b737-800x",
  deps: none,
  impact: "Adds the Boeing 737-800X as this wave's study aircraft — the aircraft the wave guide is written around.",
  notes: "User-supplied mod (freeware). Current version and any dependencies: user to provide. Per project rule, nothing here validates compatibility — confirm against 12.4.3 with the release-date heuristic.",
)

Per project rule, further wave-specific mods come from you — cards below are *pending addition*, with the fields this guide's rules demand. Anything you do not supply stays *"user to provide"*.

#mod-entry("user to provide")
