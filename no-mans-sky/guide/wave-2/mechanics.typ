#import "../helpers.typ": *

==== Wave 2: Mechanics

The endgame systems, and where the deepest mechanics mods belong.

===== Production & Manufacturing

The endgame economy is built: extraction networks, refining chains, manufacturing. Mechanics mods here add production depth — new chains, new machines, real routing decisions. The bar: a mod must make you choose what to build and where, not simply print more of what you already have.

#callout("Note:", [
  This slot is open: as of 2026-08-12 no maintained mod was found that deepens production without changing rates (Beyond Base Building's optional rate multipliers are a power spike and are intentionally left off in this modlist). See STATUS.md.
])

#mod-entry("user to provide")

===== Fleet & Freighter Depth

The fleet as a logistics puzzle: frigate specialization, mission planning, freighter operations. Mods that deepen fleet decisions fit here; mods that inflate fleet power do not.

#mod-entry(
  "Better Frigate View",
  url: "https://www.nexusmods.com/nomanssky/mods/2574",
  deps: "None",
  impact: "Pulls the frigate inspection camera back so the whole ship and its perk list fit on screen, and improves the frigate view from the freighter command panel. A readability fix for the fleet screens you live in at this wave — no stats touched.",
  notes: "by Lo2k, v2.3, updated 2025-09-06 (game 6.03).",
)

===== Settlement Systems

Running a settlement is running a small economy: construction, production, standing. Mechanics mods may deepen the settlement loop with real decisions.

#callout("Note:", [
  This slot is open: no maintained settlement mod exists for modern game builds (the few on Nexus date from 2018–2023 and predate the modding rework). See STATUS.md.
])

#mod-entry("user to provide")

===== Endgame Progression

The long chase — S-class everything, maxed tech, perfect loadouts. Mods may add new endgame progression paths that cost effort and open options.

#callout("Note:", [
  This slot is open: the maintained mods that touch endgame either remove the hunt (all-S-class generation — a pure power spike) or are abandoned. See STATUS.md for the evaluated list.
])

#mod-entry("user to provide")
